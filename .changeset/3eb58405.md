---
"aws-sdk-transcribe-streaming": minor
---

fix(auth): sign request event streams per event (`:date` + `:chunk-signature` frames chained from the request signature, plus the empty end frame); `sign_sigv4` now takes `unsigned_payload` instead of a `body` argument
